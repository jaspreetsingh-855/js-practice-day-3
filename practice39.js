39. 
/*Implement the Caesar Cipher.
(Shift each character by a fixed number)
Example (shift = 1):
Input: "abc"
Output: "bcd";
*/
function casesarCipher(text, shift){
    let result = '';
    for(let i=0; i<text.length; i++){
        let charCode = text.charCodeAt(i);
        
    }
}